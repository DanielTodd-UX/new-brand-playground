import React from 'react';
import logo from './Vertical Postive 1.svg';

const Logo = (props) => {
    return (
    <div className="container">
        <div>
        <img src={logo} className="app-logo" alt="logo" />
        </div>
    </div>
    )
};

export default Logo;